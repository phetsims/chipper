{
  "extends": "../tsconfig/buildtools/tsconfig.json",
  // this reference needs to be duplicated with the super config, because references do not extend
  "references": [
    {
      "path": "./browser-and-node/tsconfig.json"
    }
  ]
}